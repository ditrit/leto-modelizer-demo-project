name: Test CI

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  job_1:
    name: Test job
    runs-on: ubuntu-latest
    steps:
      - name: echo task
        run: echo 'Workflow start'
